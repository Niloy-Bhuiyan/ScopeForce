# Contributing

Use Node 24+ and Python 3.12 or 3.13. Create focused branches and commits, retain stable graph identifiers, and update documentation when truth boundaries change. New behavior should be tested at the deterministic domain layer before UI wiring. Run every command in the README before opening a pull request. Provider secrets belong in local or Vercel environment variables only.

